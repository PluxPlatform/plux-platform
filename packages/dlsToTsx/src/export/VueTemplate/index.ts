import { ExportConfig, NodeInfo } from "../../pageInfo";

export const exportVueTemplate = ({
  json,
  config,
}: {
  json: NodeInfo;
  config: ExportConfig;
}) => {
  return "exportVueTemplate";
};
